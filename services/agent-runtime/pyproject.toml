[project]
name = "k8s-incident-agent-runtime"
version = "0.1.0"
description = "Python agent runtime for K8s Incident Agent"
requires-python = ">=3.13.15,<3.14"
dependencies = []

[tool.uv]
required-version = "==0.12.3"
