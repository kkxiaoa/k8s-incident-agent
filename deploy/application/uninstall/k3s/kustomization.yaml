apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base/workloads
  # Removes the public Console entry when present; private K3s profiles never render it.
  - ../../platform/k3s-public
  - ../../../monitoring/uninstall/k3s
