apiVersion: apps/v1
kind: Deployment
metadata:
  name: incident-console
  namespace: k8s-incident-agent
spec:
  template:
    spec:
      containers:
        - name: console
          env:
            - name: INCIDENT_INTAKE_MODE
              value: online
