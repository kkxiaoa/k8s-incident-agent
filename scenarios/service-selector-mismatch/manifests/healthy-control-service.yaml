apiVersion: v1
kind: Service
metadata:
  name: service-selector-mismatch-healthy-control
  namespace: k8s-incident-scenarios
  labels:
    k8s-incident-agent.io/monitor-selector: "true"
spec:
  type: ClusterIP
  selector:
    app: service-selector-healthy-control
  ports:
    - name: http
      port: 80
      targetPort: 8080
